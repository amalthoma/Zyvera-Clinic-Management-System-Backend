def test_admin_route_forbidden_for_user(client):
    # This is a stub to test RBAC logic.
    # In a full test, we would mock a token for a CLINIC_USER
    # and verify they get a 403 Forbidden when accessing /api/v1/clinics
    pass
